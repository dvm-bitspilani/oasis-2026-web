# Auto-detect text files and normalize line endings to LF
* text=auto eol=lf

# Explicitly treat binary files as binary (don't touch line endings)
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary